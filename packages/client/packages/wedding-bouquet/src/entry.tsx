import * as React from "react";
import * as ReactDOM from "react-dom";

import Bouquet from "./Bouquet";

const el = document.createElement("div");
document.body.appendChild(el);

ReactDOM.render(<Bouquet />, el);
