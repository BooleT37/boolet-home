import * as React from "react";
import * as ReactDOM from "react-dom";

import Gift from "./Gift";

const el = document.createElement("div");
document.body.appendChild(el);

ReactDOM.render(<Gift />, el);
